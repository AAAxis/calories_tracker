import 'package:flutter/material.dart';
import '../ui/kali.dart';

class AppRunner extends StatelessWidget {
  const AppRunner({super.key});

  @override
  Widget build(BuildContext context) {
    return const KaliApp();
  }
}
